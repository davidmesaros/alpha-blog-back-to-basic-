
  <h2 align = "center">Title: <%= @show.title %> </h2> 
  <div class="well col-xs-8 col-xs-offset-2" >
    <ul class= 'listing'>
        <div class= "row center" >
          <li>Created By :</li>
          <% if @show.user %>
          <div class= " col-md-4 col-md-offset-4">
            <li><%= link_to gravator_for(@show.user, :size => 100), user_path(@show.user) %></li>
            <li class= 'article-title'><%= link_to @show.user.username, user_path(@show.user) %> </li>
            <li><small><%= pluralize(@show.user.articles.count, 'article') if @show.user.articles %> </small></li>
          </div>
          <% end %>
        </div>
      </ul>
    <h4 class="center"> <strong>Description:</strong></h4>
    <hr>
    <h5 class= 'description'><%= simple_format(@show.description) %></h5>
    <div class="article-action">
    <% if logged_in? && (current_user == @show.user || current_user.admin?) %>
        <%= link_to 'View List of Articles', articles_path, class: 'btn btn-ls btn-success' %> 
        <%= link_to 'Edit Article', edit_article_path(@show), class: "btn btn-ls btn-primary"  %>
        <%= link_to 'Delete Article', article_path(@show), method: :delete ,
              data: { confirm: 'Are you sure?' }, 
                  class: "btn btn-ls btn-danger"  %>
      <% end%>
    </div>
  </div>
